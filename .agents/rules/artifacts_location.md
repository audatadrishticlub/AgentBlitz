---
trigger: always_on
---

# Save Artifacts to Workspace

When working in this workspace, ALWAYS save the `implementation_plan.md` and `task.md` files directly into the root of the project directory (e.g., `c:/Users/bhara/Downloads/Data Drishti/Agent Blitz`) instead of the default `.gemini` brain directory.

